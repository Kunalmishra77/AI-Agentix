{
  "name": "ai-agentix-assistant-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend for the AI Agentix website assistant — RAG over the site's own content.",
  "scripts": {
    "build:kb": "node scripts/build-knowledge.mjs",
    "embed": "node scripts/embed-knowledge.mjs",
    "prepare:kb": "npm run build:kb && npm run embed",
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.56.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
